#include<bits/stdc++.h>
using namespace std;
void solve(int n)
{
    vector<int> ans;
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);  
    int t;
    cin >> t;
    while(t--)
    {
        int n;
        cin>>n;
        solve(n);
    }
}